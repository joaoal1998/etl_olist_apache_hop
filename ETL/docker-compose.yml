version: '3.8'

services:
  hop_web:
    build: .
    container_name: olist_hop_web
    ports:
      - "8080:8080"
    environment:
      - HOP_SERVER_PORT=8080
    volumes:
      - ./:/opt/olist_pipeline